/**
 * test env is working
 */
const expect = require('chai').expect;

describe('#ENV', function() {
    context('Mocha is working', function() {
        it('should pass anyway', function() {
            expect(1).to.equal(1);
        })
    })

})